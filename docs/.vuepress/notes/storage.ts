import { defineNoteConfig } from 'vuepress-theme-plume'

export default defineNoteConfig({
  dir: '/storage/',
  link: '/storage/',
  /*
  sidebar: [
    { text: '存储笔记', prefix: 'note', collapsed: false, items: 'auto', },
  ],
  */
})
